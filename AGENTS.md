# be 저장소 작업 지침

## 기준 문서

- 제품 요구사항과 설계 기준은 `Dynamic-Juo/docs` 레포(`project/prd.md`, `design/ai-pipeline.md`)를 따른다. 이 레포에는 기획 문서를 복사하지 않고 필요하면 링크한다.
- docs의 요구사항과 실제 구현이 달라지면(기능 추가·축소, 설계 경계 변경) 코드만 조용히 바꾸지 않고 PR 설명이나 이슈로 남겨 조정준 팀장에게 알린다. docs 자체의 기준 변경은 이 레포에서 결정하지 않는다.
- PRD·파이프라인 문서에서 아직 팀 결정이 안 나 애매한 항목(예: 팩트체크 파이프라인 포함 여부, 결과 병합 방식)은 하드 구현하지 않는다. 기본값을 명확히 정하되 옵션·플래그로 켜고 끌 수 있게 만들어서, 팀 결정이 늦게 나와도 재작업 범위를 줄인다.

## 코드 스타일

- 하드코딩된 매직 넘버·문자열을 피하되, 한 번만 쓰이는 값을 억지로 상수화하지 않는다.
- 클래스·wrapper를 추가하기 전에 함수나 모듈 수준으로 충분한지 먼저 검토한다. 상태나 캐시를 갖는 컴포넌트(모델 로더 등)는 클래스가 자연스럽다.
- 디버깅 가능성을 우선한다. 실패 지점을 감추는 과도한 추상화, 에러를 조용히 삼키는 `except: pass` 류를 피한다.
- 파이프라인 각 단계는 진행 상황과 실패 원인을 알 수 있는 로그를 남긴다. "로그 없이 넘어가는 것"을 가장 경계한다.
- 함수·모듈은 너무 잘게 쪼개서 흐름을 따라가기 어렵게 만들지 않고, 그렇다고 한 함수에 여러 책임을 몰아넣지도 않는다.

## 커밋

- Conventional Commits 형식(`<type>: <summary>`)을 쓴다. `type`은 `feat`, `fix`, `docs`, `refactor`, `test`, `perf`, `build`, `ci`, `chore` 중에서 고른다.
- 서로 무관한 변경을 한 커밋에 묶지 않는다.
- 검증했다면 실제로 실행한 검사만 커밋 메시지나 PR에 기록한다.

## 배포

- torch/torchvision은 반드시 CPU 전용 wheel index(`https://download.pytorch.org/whl/cpu`)로 설치한다. 기본 PyPI 휠은 GPU가 없는 배포 환경에도 불필요한 CUDA 런타임을 끌고 와 이미지가 커지거나 빌드가 실패한다.
- `backend/harness.py`는 job 상태를 프로세스 메모리에 들고 있으므로 uvicorn은 반드시 단일 워커 프로세스로 띄운다.
